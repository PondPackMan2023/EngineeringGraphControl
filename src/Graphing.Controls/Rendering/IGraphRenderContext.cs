namespace Graphing.Controls.Rendering
{
    /// <summary>
    /// Marker abstraction for renderer device context payloads.
    /// Platform renderers define concrete context types (WinForms, WPF, etc.).
    /// </summary>
    internal interface IGraphRenderContext
    {
    }
}